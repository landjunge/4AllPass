{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main 4AllPass window",
  "windows": ["main", "access-prompt"],
  "permissions": [
    "core:default",
    "notification:default",
    "core:event:default",
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled"
  ]
}
